@model Product
@{
    ViewData["Title"] = "@model";
}

<div class="item-box">
    <div class="info">
        <div class="breadcrumbs">
         </div>
        <h1 itemprop="name">@Model.Name</h1>
        <div class="container2">
            <div class="visual-block">
                <div class="visual image large">
                    <div class="bg-line">&nbsp;</div>

                        @Html.Partial("Image.cshtml", @Model)

                </div>
                <div class="visual video large">
                    <div class="bg-line">&nbsp;</div>
                    <div class="videowrap"></div>
                    <div class="zoomwrap">
                    </div>
                </div>
                <div class="extimgs">
                    <ul class="list">
                        @foreach (var image in Model.Images)
                        {
                            <li>
                                <img class="sm_img" src="@Url.Content("/Files/Sm/" + image.Name)" name ="@image.Name" alt="@Model.Name" title="@Model.Name" />
                            </li>
                        }
                    </ul>
                </div>
                <div id="___plusone_0"></div>
            </div>
            <div class="holder">
                <form class="order-form" id="@Model.ProductID" asp-action="AddToCart"
                      asp-controller="Cart" method="post">
                    <fieldset>
                        <input type="hidden" asp-for="ProductID" />
                        <input type="hidden" name="returnUrl"
                               value="@ViewContext.HttpContext.Request.PathAndQuery()" />
                        <input type="hidden" name="item_variant" id="variant" value="0" />
                        <input type="hidden" id="tab" name="tab" value="1" />
                        <div class="row">
                            <ul class="tab">
                                <li class="active">
                                    <a id="tab1">
                                        <span>
                                            <em>
                                                Price
                                            </em>
                                        </span>
                                    </a>
                                </li>
                            </ul>
                            <ul id="blocktab1" class="choice">
                                <li></li>
                                <li>
                                    <strong>@Model.Price$</strong>
                                </li>
                            </ul>
                        </div>
                        @*<div id="AddCart">
                            <button class="button">
                                Add to Cart
                            </button>
                            <i class="clear"></i>
                        </div>*@
                    </fieldset>
                </form>
                <div class="description">
                    <div itemprop="description" id="itemDescr">
                        <b>Descriptoins:</b><br>@Model.Description
                        <div>
                            <br>
                        </div>
                    </div>
                    
                </div>
            </div>
            <br style="clear:both;">
        </div>
        <div class="set-proposals"></div> 
        <!-- The Modal -->
        
    </div>
 
    
</div>

<div id="myModal" class="modal">

          <!-- The Close Button -->
          <span class="close">&times;</span>

          <!-- Modal Content (The Image) -->
          <img class="modal-content" id="img01">

          <!-- Modal Caption (Image Text) -->
          <div id="caption"></div>
</div>

<script>
    // Get the modal
    var modal = document.getElementById('myModal');

    // Get the image and insert it inside the modal - use its "alt" text as a caption
    var img = document.getElementById('img_Bg');
    var modalImg = document.getElementById("img01");
    var captionText = document.getElementById("caption");
    img.onclick = function () {
        modal.style.display = "block";
        modalImg.src = this.src;
        captionText.innerHTML = this.alt;
    }

    // Get the <span> element that closes the modal
    var span = document.getElementsByClassName("close")[0];

    // When the user clicks on <span> (x), close the modal
    span.onclick = function () {
        modal.style.display = "none";
    }
</script>